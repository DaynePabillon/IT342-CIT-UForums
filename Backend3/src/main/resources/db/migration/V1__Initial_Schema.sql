-- This is a baseline migration to establish the initial schema
-- It doesn't actually modify anything since the tables already exist
-- It's just to help Flyway track changes

-- This will be treated as a baseline migration that Flyway will mark as already applied
