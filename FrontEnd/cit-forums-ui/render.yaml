# Static site configuration for SPA
services:
  - type: web
    name: cit-forums-ui
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
